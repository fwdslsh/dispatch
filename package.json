{
  "name": "dispatch",
  "version": "0.1.0",
  "description": "SvelteKit PTY + Claude Code in Docker",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node build/index.js",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch"
  },
  "dependencies": {
    "@sveltejs/adapter-node": "^1.3.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.3.0",
    "express": "^4.21.2",
    "localtunnel": "^2.0.2",
    "node-pty": "^1.0.0",
    "socket.io": "^4.7.0",
    "socket.io-client": "^4.7.0"
  },
  "devDependencies": {
    "@sveltejs/kit": "^1.30.0",
    "svelte": "^4.0.0",
    "svelte-check": "^3.6.0",
    "vite": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
