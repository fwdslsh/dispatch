# Dispatch Docker Configuration with Let's Encrypt SSL

# Domain Configuration (REQUIRED for production)
# Your domain name where Dispatch will be accessible 
DOMAIN=yourdomain.com

# Terminal Access (REQUIRED)
# Strong password for accessing the Dispatch web interface
TERMINAL_KEY=change-me-to-a-strong-password

# Let's Encrypt Configuration
# Email address for Let's Encrypt certificate notifications
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Set to 1 to use Let's Encrypt staging environment (for testing)
# Set to 0 for production certificates (default)
LETSENCRYPT_STAGING=0

# Optional: LocalTunnel Configuration (not recommended with SSL)
# ENABLE_TUNNEL=false
# LT_SUBDOMAIN=my-dispatch

# Advanced Configuration (usually not needed)
# SSL_ENABLED=false  # SSL is handled by nginx reverse proxy
# TRUST_PROXY=true   # Required for proper client IP detection behind proxy